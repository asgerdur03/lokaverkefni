Verkefnakröfur:

Nota git
Use maven project layout (x)
Geta notað maven commands (kafli 4) (held að sé komið, má double checka)
    Compile
    Test
    Exec : java
    Package
    Site
JUnit test business logic behind the user interface (x)
Create a class diagram (x)
Use design patterns (skoða, bæta svo við README)
Refactoring (x)
Package a jar file (x komið held ég)
Provide jar file command (x, held að það sé ekki rétt samt)
Documentation (x)
    Licence (x)
        UML class diagram (x)
        Where pattern is used (ekki komið)
        User-end documentation (e.g how to run your jar) (x, hægt að bæta ehv við)

To-do laga:
gera design patterns (Bæta við í README file)
jar file? + jar file command


• Package a jar file and provide a command so that an end user without an IDE or Maven
can run your program (Chapter 3/Assignment 3 & 4): for running, you either need to
add for each dependency a separate jar file or let Maven create a fat jar that contains
all dependencies using the maven-assembly-plugin (do some web search as this was
not covered in this course).


